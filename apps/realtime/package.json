{
  "name": "@worqly/realtime",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json --noEmit false",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@fastify/cors": "*",
    "@worqly/shared": "0.0.0",
    "fastify": "*",
    "ioredis": "*",
    "socket.io": "*",
    "yjs": "^13.6.30",
    "zod": "*"
  },
  "devDependencies": {
    "tsx": "*",
    "typescript": "*"
  }
}
